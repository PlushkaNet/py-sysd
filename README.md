## SysD service manager for building distributed monoliths in Python — multiple services, single runtime

Quick installation from GitHub using pip:

```
pip install git+https://github.com/PlushkaNet/py-sysd.git
```